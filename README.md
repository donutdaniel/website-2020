# Personal Website

## Local deploy:
* ```gatsby develop```
* go to localhost:8000

## Build:
* to create prod-quality build
* ```gatsby build```
* go to localhost:8000

## Deploy to gh-pages at donutdaniel.github.io
* ```npm run deploy```